import React from 'react';
import Navigation from './Navigation';

const AddNewNotePage = () => {
  return (
    <div>
      <Navigation />
      <h1>Add New Note Page</h1>
      {/* Add new note page content */}
    </div>
  );
};

export default AddNewNotePage;
